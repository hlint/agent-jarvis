- [ ] 让AI的工作区可以化身web服务，初步想法，类似于nextjs，每个文件是一个路由，文件名是路由路径，文件内容是路由处理函数
- [x] 更好的工作区脚本传参方式，统一json格式，由系统转义为命令行参数，这是为web服务传参做准备。
- [x] 技能允许设置为常驻（active=true），常驻技能在AI启动时自动加载，非常驻技能在需要时加载
- [x] 写日记、加载日记
- [ ] 工作区文件搜索工具
- [x] callLLM报错的处理
- [x] 定时触发事件，允许AI主动发起对话
- [x] 上下文清理和压缩
- [x] 使用ai库，而不是openai，支持更多模型
- ~~Think模型使用强制工具而不是自定义文档解析，确保正确生成了action~~（搁置，因为会让AI误解输出格式）
