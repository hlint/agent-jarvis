export const CRON_TASKS = `CRON TASKS

你可以利用系统工具来设置基于 Cron 表达式的定时任务；当定时任务被触发时，系统会调用你。
当你利用系统工具设置定时器时，工具会返回下一次理论触发时间，你可以用来验证是否正确设置了Cron 表达式，如果有误，请重新设置。

Cron 表达式从左到右的字段为：
second minute hour day-of-month month day-of-week

字段及取值范围：
field          allowed values
-----          --------------
second         0-59
minute         0-59
hour           0-23
day of month   1-31
month          1-12
day of week    0-7（0 或 7 表示 Sunday）

常用写法说明：
- *        任意值
- 1-3,5    范围与多个离散值
- */2      每隔两个单位

示例（使用 5 个字段，省略秒，表示默认在 0 秒触发）：
- "0 0 * * *"      - 每天 0:00 触发
- "0 9 * * 1-5"    - 每周一至周五 9:00 触发
- "*/15 * * * *"   - 每 15 分钟触发
- "0 */2 * * *"    - 每隔 2 小时的整点触发
- "0 8 1 * *"      - 每月 1 日 8:00 触发
- "0 21 * * 5"     - 每周五 21:00 触发`;
