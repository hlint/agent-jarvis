export { DEFAULT_WHITEBOARD_PATH as WHITEBOARD_HOME_PATH } from "@repo/shared/defines/constant";

/** postMessage type from whiteboard iframe → parent (keep in sync with bootstrap.js) */
export const WHITEBOARD_NAVIGATE_MESSAGE_TYPE = "jarvis:whiteboard-navigate";
